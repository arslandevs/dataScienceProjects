-- create table cats(
--     cat_id int not null auto_increment,
--     name varchar(50) default 'unknown',
--     AGE int,
--     primary key(cat_id)
-- );


select substr(
    replace(title, 'e', 3), 1,5) as  'weird_titles'
    from books;


























